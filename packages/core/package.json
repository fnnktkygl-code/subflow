{
  "name": "@subflow/core",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@subflow/tsconfig": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
